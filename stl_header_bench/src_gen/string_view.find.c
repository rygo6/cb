/* Generated by stl_header_bench/run.py */
#include <string.h>

__attribute__((noinline)) int c_case_string_view_find(int n, int *out)
{

            const char *s = "cflat";
            const char *found = memchr(s, 'a', strlen(s));
            int value = (found ? (int)(found - s) : (int)strlen(s)) + n;
            *out = value;
            return value;
        
}
