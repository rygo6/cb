// Generated by stl_header_bench/run.py
#include <compare>

struct CmpBox { int v; auto operator<=>(const CmpBox&) const = default; };

extern "C" __attribute__((noinline)) int cpp_case_compare_spaceship_ordering(int n, int *out)
{

            CmpBox a{n};
            CmpBox b{n + 1};
            auto order = a <=> b;
            int value = (order < 0) ? 1 : 0;
            value += std::is_lt(order) ? 2 : 0;
            *out = value;
            return value;
        
}
