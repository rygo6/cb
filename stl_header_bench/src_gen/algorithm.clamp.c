/* Generated by stl_header_bench/run.py */

__attribute__((noinline)) int c_case_algorithm_clamp(int n, int *out)
{

            int value = n < 1 ? 1 : (n > 4 ? 4 : n);
            value += (n + 3) < 1 ? 1 : ((n + 3) > 8 ? 8 : (n + 3));
            *out = value;
            return value;
        
}
