/* Generated by stl_header_bench/run.py */

__attribute__((noinline)) int c_case_algorithm_sort(int n, int *out)
{

            int a[4] = {n, 4, 1, 2};
            for (int i = 1; i < 4; ++i) {
                int key = a[i];
                int j = i - 1;
                while (j >= 0 && a[j] > key) { a[j + 1] = a[j]; --j; }
                a[j + 1] = key;
            }
            int value = a[0] + a[1] + a[2] + a[3];
            *out = value;
            return value;
        
}
