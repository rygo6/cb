/* Generated by stl_header_bench/run.py */

__attribute__((noinline)) int c_case_algorithm_binary_search(int n, int *out)
{

            int a[4] = {1, 2, 4, 8};
            int needle = (n & 1) ? 4 : 8;
            int lo = 0, hi = 4;
            while (lo < hi) { int mid = lo + (hi - lo) / 2; if (a[mid] < needle) lo = mid + 1; else hi = mid; }
            int value = (lo < 4 && a[lo] == needle) ? 1 : 0;
            *out = value;
            return value;
        
}
