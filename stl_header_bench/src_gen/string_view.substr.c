/* Generated by stl_header_bench/run.py */
#include <string.h>

__attribute__((noinline)) int c_case_string_view_substr(int n, int *out)
{

            const char *s = "cflat";
            size_t len = strlen(s);
            const char *sub = s + 1;
            size_t sub_len = 2u < len - 1u ? 2u : len - 1u;
            int value = (int)sub_len + sub[0] + n;
            *out = value;
            return value;
        
}
